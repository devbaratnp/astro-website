import { useEffect, useState, useRef } from 'react';
import { ChartBar, CalendarCheck, HandsPraying, CreditCard, Envelope, BookOpen, SignOut, List, Upload, X } from '@phosphor-icons/react';
import '../admin.css';
const API='/backend/api';
const sections=[['dashboard','Dashboard',ChartBar],['appointments','Appointments',CalendarCheck],['pooja','Pooja orders',HandsPraying],['payments','Payments',CreditCard],['messages','Messages',Envelope],['services','Pooja services',List],['articles','Articles',BookOpen],['events','Events & Tours',CalendarCheck],['gallery','Gallery',BookOpen],['testimonials','Testimonials',ChartBar],['rewards','Rewards',ChartBar],['panchang','Panchang',CalendarCheck]];
const editors={services:[['title_ne','Nepali title'],['title_en','English title'],['category','Category'],['base_price','Price'],['duration_minutes','Duration']],articles:[['title_ne','Nepali title'],['slug','URL slug'],['excerpt_ne','Excerpt'],['content_ne','Content']],rewards:[['user_name','User name'],['user_phone','Phone'],['reward_type','Type'],['title_ne','Title'],['expires_at','Expires']],panchang:[['date','Date'],['tithi','Tithi'],['nakshatra','Nakshatra'],['sunrise','Sunrise'],['sunset','Sunset'],['special_events_ne','Special events']],testimonials:[['name','Name'],['title','Title'],['content','Content'],['rating','Rating (1-5)'],['location','Location'],['sort_order','Sort order']],events:[['type','Type (event/tour)'],['title_ne','Nepali title'],['title_en','English title'],['date_from','Date from'],['location','Location'],['contact_person','Contact person'],['contact_phone','Contact phone']],gallery:[['type','Type (image/video/audio)'],['title_ne','Nepali title'],['url','URL'],['thumbnail','Thumbnail URL'],['embed_url','Embed URL'],['source','Source']]};
const imageFields={articles:['cover_image'],events:['cover_image'],gallery:['url','thumbnail'],testimonials:['photo']};
async function api(path,options={}){const response=await fetch(`${API}/${path}`,{credentials:'same-origin',headers:{'Content-Type':'application/json'},...options});const body=await response.json().catch(()=>({success:false,message:`Server error (${response.status})`}));if(!body.success)throw new Error(body.message||'Request failed');return body.data}
export function Admin(){const [user,setUser]=useState(null),[loading,setLoading]=useState(true),[section,setSection]=useState('dashboard'),[data,setData]=useState(null),[error,setError]=useState(''),[refresh,setRefresh]=useState(0),[editing,setEditing]=useState(null),[credentials,setCredentials]=useState({username:'',password:''});useEffect(()=>{api('auth.php').then(x=>setUser(x.user)).catch(()=>{}).finally(()=>setLoading(false))},[]);useEffect(()=>{if(!user)return;setData(null);setError('');setEditing(null);api(`admin.php?resource=${section}`).then(setData).catch(e=>setError(e.message))},[user,section,refresh]);async function login(e){e.preventDefault();setError('');try{const x=await api('auth.php',{method:'POST',body:JSON.stringify(credentials)});setUser(x.user)}catch(e){setError(e.message)}}async function logout(){await api('auth.php?logout=1');setUser(null)}async function mutate(method,payload){try{await api(`admin.php?resource=${section}`,{method,body:JSON.stringify(payload)});setRefresh(x=>x+1)}catch(e){setError(e.message)}}async function save(e){e.preventDefault();const payload=Object.fromEntries(new FormData(e.currentTarget));if(editing?.id)payload.id=editing.id;await mutate(editing?.id?'PUT':'POST',payload)}if(loading)return <div className="admin-loading">Loading…</div>;if(!user)return <div className="admin-login"><form onSubmit={login}><div className="admin-symbol">ॐ</div><h1>Admin login</h1><p>Shreehari management system</p>{error&&<div className="admin-error">{error}</div>}<label>Username<input value={credentials.username} onChange={e=>setCredentials({...credentials,username:e.target.value})} required/></label><label>Password<input type="password" value={credentials.password} onChange={e=>setCredentials({...credentials,password:e.target.value})} required/></label><button>Log in</button><a href="/">← Back to website</a></form></div>;return <div className="admin-shell"><aside><div className="admin-brand"><b>ॐ</b><span>Shreehari Admin<small>Management system</small></span></div><nav>{sections.map(([key,label,Icon])=><button key={key} className={section===key?'active':''} onClick={()=>setSection(key)}><Icon/>{label}</button>)}</nav><div className="admin-user"><button onClick={logout}><SignOut/>Log out</button></div></aside><main><div className="admin-topbar"><h2>{sections.find(s=>s[0]===section)?.[1]||section}</h2><span>{editing?'Editing…':''}</span></div>{error&&<div className="admin-error">{error}</div>}{section==='dashboard'?<Dashboard data={data}/>:<><div className="admin-toolbar">{editors[section]&&<button className="button button-gold" onClick={()=>setEditing({})}>{editing?'New':'Add'}</button>}</div>{editing!==null&&<Editor fields={editors[section]} value={editing.id?editing:null} imageFields={imageFields[section]||[]} section={section} onSubmit={save} onCancel={()=>setEditing(null)}/>}<DataTable rows={data} section={section} onEdit={r=>setEditing(r)} onMutate={mutate}/></>}</main></div>}
function Dashboard({data}){return <div className="admin-stats">{Object.entries(data).map(([k,v])=><article key={k}><strong>{v}</strong><span>{k.replaceAll('_',' ')}</span></article>)}</div>}
function FileUpload({name,value,onChange,accept='image/*'}){const[drag,setDrag]=useState(false);const[preview,setPreview]=useState(value||'');const inputRef=useRef(null);async function upload(file){const fd=new FormData();fd.append('file',file);fd.append('type','general');try{const res=await fetch(`${API}/upload.php`,{method:'POST',credentials:'same-origin',body:fd});const d=await res.json();if(d.success){setPreview(d.data.url);onChange(d.data.url)}}catch(e){}}function handleDrop(e){e.preventDefault();setDrag(false);const file=e.dataTransfer.files[0];if(file)upload(file)}function handleFile(e){const file=e.target.files[0];if(file)upload(file)}return<div className="file-upload-wrap"><div className={`file-zone${drag?' drag-over':''}`}onDragOver={e=>{e.preventDefault();setDrag(true)}}onDragLeave={()=>setDrag(false)}onDrop={handleDrop}onClick={()=>inputRef.current?.click()}>{preview?<img src={preview} alt="preview" className="file-preview"/>:<span><Upload/>तस्बिर राख्नको लागि यहाँ छोड्नुहोस् वा क्लिक गर्नुहोस्</span>}</div><input ref={inputRef} type="file" accept={accept} style={{display:'none'}}onChange={handleFile}/>{preview&&<button type="button" className="file-clear" onClick={()=>{setPreview('');onChange('')}}><X/></button>}</div>}
function Editor({fields,value,onSubmit,onCancel,imageFields,section}){const[uploads,setUploads]=useState({});function handleChange(name,val){setUploads(p=>({...p,[name]:val}))}return<form className="admin-editor" onSubmit={onSubmit}><h3>{value?'Edit record':'Add record'}</h3><div>{fields.map(([name,label])=><label key={name} className={imageFields?.includes(name)?'image-field':''}>{label}{imageFields?.includes(name)?<FileUpload name={name} value={value?.[name]||''} onChange={v=>handleChange(name,v)}/>:<input name={name} defaultValue={value?.[name]??''} required={!imageFields?.includes(name)}/>}</label>)}</div>{Object.entries(uploads).map(([k,v])=><input key={k} type="hidden" name={k} value={v}/>)}<button>{value?'Update':'Save'}</button>{value&&<button type="button" className="secondary" onClick={onCancel}>Cancel</button>}</form>}
function DataTable({rows,section,onEdit,onMutate}){if(!rows?.length)return <div className="admin-empty">No records yet.</div>;const hidden=['admin_notes','message','content_ne','content_en','description_ne','description_en','password_hash'];const keys=Object.keys(rows[0]).filter(k=>!hidden.includes(k)).slice(0,6);const statuses={appointments:['pending','confirmed','completed','cancelled'],pooja:['pending','confirmed','completed','cancelled'],payments:['pending','approved','rejected']};return <div className="admin-table-wrap"><table><thead><tr>{keys.map(k=><th key={k}>{k.replaceAll('_',' ')}</th>)}<th>Actions</th></tr></thead><tbody>{rows.map((r,i)=><tr key={r.id||i}>{keys.map(k=><td key={k}>{typeof r[k]==='string'&&r[k].startsWith('http')?<img src={r[k]} alt="" className="cell-thumb"/>:String(r[k]??'—')}</td>)}<td className="admin-actions">{statuses[section]&&<select value={r.status} onChange={e=>onMutate('PATCH',{id:r.id,status:e.target.value})}>{statuses[section].map(s=><option key={s}>{s}</option>)}</select>}{section==='messages'&&<button onClick={()=>onMutate('PATCH',{id:r.id,is_read:r.is_read?0:1})}>{r.is_read?'Unread':'Read'}</button>}{section==='rewards'&&<button onClick={()=>onMutate('PATCH',{id:r.id,is_redeemed:r.is_redeemed?0:1})}>{r.is_redeemed?'Reactivate':'Redeem'}</button>}{editors[section]&&<><button onClick={()=>onEdit(r)}>Edit</button><button className="danger" onClick={()=>confirm('Delete this record?')&&onMutate('DELETE',{id:r.id})}>Delete</button></>}</td></tr>)}</tbody></table></div>}
